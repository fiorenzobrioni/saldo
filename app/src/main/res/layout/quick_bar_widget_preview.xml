<?xml version="1.0" encoding="utf-8"?>
<!--
    Static preview of the bar widget for the picker. Two labelled buttons side by
    side, the same glyphs and accents the real widget draws. Same colour rules as
    quick_add_widget_preview.xml: own resources with a values-night variant,
    never ?android:attr, which a previewLayout resolves against the light
    DeviceDefault theme whatever the launcher is wearing. The icons are plain
    vectors on purpose: the adaptive shortcut icons used here before rendered
    unreliably in the picker (a full squircle on one button, nothing on the other).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_preview_background"
    android:gravity="center"
    android:orientation="horizontal"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:contentDescription="@null"
            android:src="@drawable/ic_widget_preview_expense" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:maxLines="1"
            android:text="@string/widget_quick_add_expense"
            android:textColor="@color/widget_preview_expense"
            android:textSize="14sp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:contentDescription="@null"
            android:src="@drawable/ic_widget_preview_income" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:maxLines="1"
            android:text="@string/widget_quick_add_income"
            android:textColor="@color/widget_preview_income"
            android:textSize="14sp" />
    </LinearLayout>
</LinearLayout>
