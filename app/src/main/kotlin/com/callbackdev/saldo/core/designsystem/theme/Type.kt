package com.callbackdev.saldo.core.designsystem.theme

import androidx.compose.material3.Typography

/** Material 3 default type scale; customize per-style as the design evolves. */
val SaldoTypography = Typography()
